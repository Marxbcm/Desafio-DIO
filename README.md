# 📘 Exercícios Básicos em Python

Este repositório contém **três exercícios introdutórios em Python**, focados em lógica de programação, entrada de dados, condicionais, operadores aritméticos e manipulação de strings. Eles são ideais para quem está começando a aprender a linguagem.

---

## 1️⃣ Verificação de Número Par ou Ímpar

Neste exercício, o programa recebe um **número inteiro** informado pelo usuário e verifica se ele é **par ou ímpar**.

### Conceitos abordados:

* Entrada de dados com `input()`
* Conversão de tipos (`int`)
* Estruturas condicionais (`if` e `else`)
* Operador módulo (`%`)

### Lógica utilizada:

Um número é considerado **par** quando o resto da divisão por 2 é igual a zero. Caso contrário, ele é **ímpar**.

---

## 2️⃣ Cálculo da Média de Três Notas

Neste exercício, o programa solicita ao usuário **três notas** e calcula a **média aritmética** entre elas.

### Conceitos abordados:

* Entrada de dados do usuário
* Conversão para números decimais (`float`)
* Operadores aritméticos (`+` e `/`)
* Cálculo de média

### Lógica utilizada:

A média é obtida somando as três notas e dividindo o resultado por 3.

---

## 3️⃣ Verificação de Palíndromo

Neste exercício, o programa verifica se uma **palavra é um palíndromo**, ou seja, se pode ser lida da mesma forma de trás para frente.

### Conceitos abordados:

* Manipulação de strings
* Fatiamento de strings
* Comparação de valores
* Estruturas condicionais

### Lógica utilizada:

A palavra digitada é invertida utilizando fatiamento de string (`[::-1]`) e comparada com a palavra original. Se forem iguais, trata-se de um palíndromo.

---

## 🎯 Objetivo dos Exercícios

Esses exercícios têm como objetivo fortalecer a **base lógica** do programador iniciante, preparando-o para desafios mais avançados em Python e em outras linguagens de programação.

---

🚀 *Bons estudos e boa programação!*
